import 'package:flutter/material.dart';

class Hiremi360screen extends StatelessWidget {
  const Hiremi360screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hiremi 360 Screen'),
      ),
    );
  }
}