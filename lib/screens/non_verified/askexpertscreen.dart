import 'package:flutter/material.dart';

class Askexpertscreen extends StatelessWidget {
  const Askexpertscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ask Expert Screen'),
      ),
    );
  }
}
